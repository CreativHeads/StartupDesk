import { ArrowRight, ArrowUpRight, Check, FileText, Home, Layers3, ShieldCheck } from "lucide-react";
import { Link, useParams } from "react-router-dom";
import SiteHeader from "@/components/SiteHeader";
import SiteFooter from "@/components/SiteFooter";
import { services } from "@/lib/siteData";

export default function ServiceDetail() {
  const { id } = useParams();
  const service = services.find((item) => item.slug === id) ?? services[0];
  return <div className="min-h-screen bg-[#fafafa] text-slate-950" data-testid={`service-detail-page-${service.slug}`}><SiteHeader /><main className="pt-20"><section className="detail-hero" data-testid="service-detail-hero"><div className="detail-hero-image" style={{ backgroundImage: `url(${service.image})` }} /><div className="detail-hero-overlay" /><div className="relative mx-auto grid max-w-7xl gap-10 px-4 py-24 sm:px-6 lg:grid-cols-[1fr_0.7fr] lg:items-end lg:px-8 lg:py-32"><div className="text-white"><div className="eyebrow eyebrow-light" data-testid="service-detail-eyebrow"><span className="eyebrow-dot" />{service.category} / StartupDesk advisory</div><h1 className="mt-7 max-w-3xl font-heading text-5xl font-extrabold leading-[1] tracking-[-0.06em] sm:text-6xl" data-testid="service-detail-heading">{service.title}</h1><p className="mt-6 max-w-xl text-lg leading-8 text-slate-200" data-testid="service-detail-description">{service.description}</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Link to="/contact" className="button-accent" data-testid="service-detail-cta">Get a tailored quote <ArrowUpRight size={17} /></Link><Link to="/services" className="button-ghost-light" data-testid="service-detail-back-link">All jurisdictions <ArrowRight size={17} /></Link></div></div><div className="detail-price-card" data-testid="service-detail-price-card"><p className="eyebrow eyebrow-light" data-testid="service-detail-price-label">Indicative package from</p><p className="mt-3 font-heading text-4xl font-extrabold tracking-[-0.06em]" data-testid="service-detail-price">{service.price}</p><p className="mt-3 text-sm leading-6 text-slate-400">Final pricing depends on activities, visas, workspace, and authority requirements.</p></div></div></section><section className="border-b border-slate-200 bg-white" data-testid="service-detail-stats"><div className="mx-auto grid max-w-7xl gap-5 px-4 py-7 sm:grid-cols-3 sm:px-6 lg:px-8">{service.stats.map((stat) => <div key={stat.label} className="flex items-center justify-between border-b border-slate-200 pb-5 sm:border-b-0 sm:border-r sm:pb-0 sm:pr-6 last:border-0"><span className="text-xs font-bold uppercase tracking-[0.14em] text-slate-500" data-testid={`service-stat-label-${stat.label.toLowerCase().replaceAll(" ", "-")}`}>{stat.label}</span><span className="font-heading text-xl font-extrabold tracking-tight text-slate-950" data-testid={`service-stat-value-${stat.label.toLowerCase().replaceAll(" ", "-")}`}>{stat.value}</span></div>)}</div></section><section className="section-pad" data-testid="service-overview-section"><div className="mx-auto grid max-w-7xl gap-14 px-4 sm:px-6 lg:grid-cols-[0.8fr_1.2fr] lg:px-8"><div><p className="eyebrow" data-testid="service-overview-eyebrow">The StartupDesk view</p><h2 className="section-title" data-testid="service-overview-heading">Structure first.<br /><span className="text-emerald-600">Momentum next.</span></h2></div><div><p className="text-xl leading-9 text-slate-700" data-testid="service-overview-text">{service.overview}</p><div className="mt-9 grid gap-3 sm:grid-cols-2">{service.benefits.map((benefit, index) => <div key={benefit} className="flex items-center gap-3 border-t border-slate-200 pt-4 text-sm font-semibold text-slate-800" data-testid={`service-benefit-${index + 1}`}><Check size={16} className="text-emerald-600" />{benefit}</div>)}</div></div></div></section><section className="section-pad bg-[#0b0f19] text-white" data-testid="service-options-section"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="section-heading-row"><div><p className="eyebrow eyebrow-dark" data-testid="service-options-eyebrow">Built around your business</p><h2 className="section-title text-white" data-testid="service-options-heading">Licenses, spaces,<br /><span className="text-emerald-300">smart choices.</span></h2></div><ShieldCheck className="hidden text-emerald-300/70 sm:block" size={70} /></div><div className="mt-14 grid gap-4 lg:grid-cols-3">{service.licenses.map((item, index) => <article className="dark-info-card" key={item.title} data-testid={`service-license-option-${index + 1}`}><FileText size={20} className="text-emerald-300" /><h3 className="mt-7 font-heading text-xl font-bold tracking-tight" data-testid={`service-license-option-title-${index + 1}`}>{item.title}</h3><p className="mt-3 text-sm leading-6 text-slate-400" data-testid={`service-license-option-description-${index + 1}`}>{item.description}</p></article>)}</div><div className="mt-14 grid gap-4 lg:grid-cols-3">{service.workspaces.map((item, index) => <article className="dark-info-card dark-info-card-muted" key={item.title} data-testid={`service-workspace-option-${index + 1}`}><Home size={20} className="text-emerald-300" /><h3 className="mt-7 font-heading text-xl font-bold tracking-tight" data-testid={`service-workspace-option-title-${index + 1}`}>{item.title}</h3><p className="mt-3 text-sm leading-6 text-slate-400" data-testid={`service-workspace-option-description-${index + 1}`}>{item.description}</p></article>)}</div></div></section><section className="section-pad" data-testid="service-process-section"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="max-w-xl"><p className="eyebrow" data-testid="service-process-eyebrow">How it works</p><h2 className="section-title" data-testid="service-process-heading">A measured route<br />to <span className="text-emerald-600">go-live.</span></h2></div><div className="mt-12 grid gap-4 md:grid-cols-3">{service.process.map((step, index) => <div className="process-card" key={step} data-testid={`service-process-step-${index + 1}`}><span className="font-mono text-sm font-bold text-emerald-600">0{index + 1}</span><Layers3 className="mt-10 text-slate-300" size={24} /><h3 className="mt-5 font-heading text-xl font-bold tracking-tight" data-testid={`service-process-title-${index + 1}`}>{step}</h3></div>)}</div></div></section><section className="section-pad border-t border-slate-200 bg-white" data-testid="service-requirements-section"><div className="mx-auto grid max-w-7xl gap-12 px-4 sm:px-6 lg:grid-cols-[0.7fr_1.3fr] lg:px-8"><div><p className="eyebrow" data-testid="requirements-eyebrow">Prepare with confidence</p><h2 className="section-title" data-testid="requirements-heading">What we need<br />from <span className="text-emerald-600">you.</span></h2><p className="mt-5 text-sm leading-6 text-slate-600" data-testid="requirements-description">Requirements can vary by authority and activity. We will confirm the exact checklist before anything is filed.</p></div><div className="grid gap-4 sm:grid-cols-2">{service.requirements.map((item, index) => <div className="requirement-card" key={item} data-testid={`requirement-item-${index + 1}`}><span className="requirement-number">0{index + 1}</span><p className="text-sm font-semibold leading-6 text-slate-800">{item}</p></div>)}</div></div></section><section className="section-pad pt-0"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="cta-banner"><div><p className="eyebrow eyebrow-light" data-testid="service-final-cta-eyebrow"><span className="eyebrow-dot" />One clear next step</p><h2 className="mt-5 max-w-xl font-heading text-4xl font-extrabold tracking-[-0.05em] text-white" data-testid="service-final-cta-heading">Let's make this structure work for you.</h2><Link to="/contact" className="button-accent mt-8" data-testid="service-final-cta-button">Talk to StartupDesk <ArrowUpRight size={17} /></Link></div></div></div></section></main><SiteFooter /></div>;
}
